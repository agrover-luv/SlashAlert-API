# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# .env.local is gitignored and won't be committed to the repository

# Azure Cosmos DB Configuration
COSMOS_DB_ENDPOINT=https://your-cosmos-account.documents.azure.com:443/
COSMOS_DB_PRIMARY_KEY=your-primary-key-here

# Note: In production, these are set by GitHub Secrets via the deployment pipeline
# In development, you can either:
# 1. Create appsettings.Local.json with these values (recommended)
# 2. Set these environment variables locally
# 3. Use any other secure local configuration method